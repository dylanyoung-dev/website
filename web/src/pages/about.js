import Layout from '../components/Layout';

const AboutPage = ({}) => {
    return (
        <Layout metaTitle="" metaDescription="">
            Coming Soon
        </Layout>
    );
};

export default AboutPage;
