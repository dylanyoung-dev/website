import Layout from '../components/ui/Layout';

const ContactPage = ({}) => {
    return (
        <Layout metaTitle="" metaDescription="">
            Coming Soon
        </Layout>
    );
};

export default ContactPage;
