import Layout from '../components/Layout';

const ContactPage = ({}) => {
    return (
        <Layout metaTitle="" metaDescription="">
            Coming Soon
        </Layout>
    );
};

export default ContactPage;
